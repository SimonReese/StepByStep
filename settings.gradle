pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //Aggiunta per grafici
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MiaProvaWidget"
include ':app'
